document.addEventListener('DOMContentLoaded', () => {
    const contenido = document.getElementById('contenido');
    contenido.innerHTML = '<h2>Seleccione su tipo de cliente para ver precios.</h2>';
    // Simulación básica
});